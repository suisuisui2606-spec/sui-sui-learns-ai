# 入门路线

这是一条适合初学者的 12 周慢学习路线。每周只抓一两个重点，目标是建立感觉，而不是赶进度。

## 第 1 周：认识 C 程序

- 看懂 `#include <stdio.h>`、`int main(void)`、`return 0;` 的大概作用。
- 会写并运行一个 `printf` 程序。
- 知道「写代码 -> 编译 -> 运行」这三个动作。

小练习：改一改 `examples/hello_suisui.c` 里的输出文字。

## 第 2 周：变量和类型

- 认识 `int`、`char`、`float`、`double`。
- 理解变量像有名字的盒子。
- 练习打印不同类型的数据。

小练习：记录年龄、身高、一个喜欢的字母。

## 第 3 周：表达式和运算

- 练习 `+`、`-`、`*`、`/`、`%`。
- 理解整数除法和小数除法的差别。

小练习：写一个简单的加减乘除计算器。

## 第 4 周：输入和输出

- 使用 `printf` 输出。
- 初步使用 `scanf` 输入。
- 认识格式占位符，比如 `%d`、`%f`、`%c`。

小练习：输入名字或年龄，输出一句问候。

## 第 5 周：条件判断

- 学习 `if` 和 `else`。
- 认识比较运算符：`==`、`!=`、`>`、`<`、`>=`、`<=`。

小练习：输入温度，判断今天穿厚一点还是薄一点。

## 第 6 周：循环

- 学习 `for` 和 `while`。
- 理解「重复」和「停止条件」。

小练习：从 1 数到 10，或者打印一张小乘法表。

## 第 7 周：函数

- 把重复代码整理成函数。
- 理解参数和返回值。

小练习：写一个 `add` 函数，返回两个整数的和。

## 第 8 周：数组

- 理解数组是一排同类型数据。
- 练习下标从 0 开始。

小练习：保存一周每天的气温，计算平均值。

## 第 9 周：字符串

- 认识字符数组。
- 理解 C 字符串末尾的 `'\0'`。

小练习：输入一个简短名字并打印问候语。

## 第 10 周：指针初见

- 理解地址和值的区别。
- 认识 `&` 和 `*`。

小练习：打印一个变量的值和地址。

## 第 11 周：结构体

- 用 `struct` 描述一个人、一件物品或一条成长记录。

小练习：定义一个 `BabyRecord` 结构体，包含年龄、身高、体重。

## 第 12 周：第一个小项目

- 把变量、条件、循环、函数、结构体组合起来。
- 做一个简单的「成长记录」命令行程序。

小练习：输入多条成长记录，显示最高身高或平均体重。

## 每周固定动作

- 写一个最小程序。
- 改 3 个地方观察结果。
- 记录一个问题。
- 记录一个突然懂了的瞬间。
